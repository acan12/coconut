<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/gelpen</item>
    </style>

    <style name="GelPenFontStyle" parent="android:TextAppearance">
        <!-- Custom Attr-->
        <item name="android:fontFamily">@font/gelpen</item>
    </style>

    <style name="VolteRoundedFontStyle" parent="android:TextAppearance">
        <!-- Custom Attr-->
        <item name="android:fontFamily">@font/volte_rounded_bold</item>
    </style>

</resources>
